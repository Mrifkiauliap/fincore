feat: OCR & Voice resilience, deferred VN processing, re-analyze flow

Phase 1-2: Fix stuck processingStatus on permanent failure, add
5s exponential retry backoff for OCR (Gemini) and Voice (Groq),
circuit breaker (5 failures → 60s OPEN), Sumopod Vision OCR
fallback via gpt-4.1-nano ($0.10/$0.40 per 1M tokens).

Phase 3: Re-analyze failed messages via WA reply "ulangi" and
Dashboard 🗗 Retry button. New GET /api/logs (paginated, search,
filter) and POST /api/logs/retry endpoints.

Phase 5: Deferred VN processing — Voice Notes without caption
set pending_confirmation, save transcript, ask user for context.
Reply with context merges into AI extraction without re-transcribing.

Files:
- New: sumopod-vision.provider.ts, circuit-breaker.ts,
  /api/logs/route.ts, /api/logs/retry/route.ts
- Modified: image-ocr.processor.ts, voice-transcription.processor.ts,
  message.processor.ts, packages/config, packages/ai, system/page.tsx,
  dashboard/package.json
- Docs: 07-checkpoint.md
