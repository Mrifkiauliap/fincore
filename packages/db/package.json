{
  "name": "@fincore/db",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "db:push": "drizzle-kit push",
    "db:seed": "tsx src/seed.ts",
    "clean": "rm -rf tsconfig.tsbuildinfo drizzle"
  },
  "dependencies": {
    "@fincore/config": "workspace:*",
    "@fincore/contracts": "workspace:*",
    "@fincore/shared": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/pg": "^8.11.0",
    "drizzle-kit": "^0.30.5",
    "tsx": "^4.7.1",
    "typescript": "^5.3.3"
  }
}
